Steps:
run python server.py in one terminal
run python client.py in another
Enter a string in client and press Enter.


Explanation:
This is a basic distributed computation example. The client sends data to the server to compute, and it is sent back.